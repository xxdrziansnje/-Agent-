# ============================================================
# 智扫通 - 环境变量配置
# ============================================================
# 使用方法：
#   1. 复制此文件为 .env
#      Windows PowerShell:  Copy-Item .env.example .env
#      macOS / Linux:       cp .env.example .env
#
#   2. 用记事本或编辑器打开 .env，把下面的 DASHSCOPE_API_KEY 改成你自己的
#
#   3. 保存后重新运行程序即可，不需要手动设置系统环境变量
#      （代码中已通过 python-dotenv 自动读取此文件）
#
# 为什么需要这个文件？
#   本项目使用阿里云「通义千问」大模型和「DashScope」向量模型。
#   这些模型的调用需要通过 API Key 鉴权，就像你登录网站需要账号密码一样。
#   API Key 是你的「通行证」，阿里云用它来识别是谁在调用、扣谁的费用。
#   把它写在 .env 文件里而不是代码里，是为了安全 —— .env 不会被提交到 Git。
# ============================================================

# 【必填】阿里云 DashScope API Key
# 为什么需要？
#   通义千问大模型 (qwen3-max) 和文本向量模型 (text-embedding-v4)
#   都通过 DashScope 平台调用，API Key 是唯一的身份凭证。
#
# 如何获取？
#   1. 打开 https://dashscope.console.aliyun.com/
#   2. 用你的阿里云账号登录
#   3. 左侧菜单 → API-KEY 管理 → 创建新的 API-KEY
#   4. 复制生成的 sk-xxxxx 填到下面
#
DASHSCOPE_API_KEY=sk-your-api-key-here

# ============================================================
# 关于 BASE_URL（可选）
# ============================================================
# 本项目使用的是阿里云官方 SDK（langchain_community 的 ChatTongyi），
# 它会自动连接阿里云 DashScope 的官方接口，不需要手动配置 base_url。
#
# 如果你使用的是其他兼容 OpenAI 接口的第三方服务（比如代理站），
# 则需要修改 model/factory.py 中的代码，添加 base_url 参数：
#
#   from langchain_openai import ChatOpenAI
#   chat_model = ChatOpenAI(
#       model="qwen3-max",
#       api_key="your-key",
#       base_url="https://your-proxy-url/v1",  # 第三方服务的地址
#   )
#
# 一般情况下不需要改，直接用阿里云官方接口即可。
# ============================================================
